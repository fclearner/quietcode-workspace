{
  "name": "personal-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "A dependency-free personal practice workspace",
  "scripts": {
    "import": "node scripts/import-data.js",
    "audit": "node scripts/audit-content.js",
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
